/* In this program the - can be used as a unary operator to reverse the sign of its operand. */

#include <stdio.h>

int main(void)
{
    int i;

    i = 10;
    i = -i;
    printf("This is i: %d", i);

    return 0;
}
